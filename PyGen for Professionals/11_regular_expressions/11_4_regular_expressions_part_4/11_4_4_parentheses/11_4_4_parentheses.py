'''
TODO:
    Complete the code below so that the regex variable contains a regular
    expression that matches lines that contain an opening parenthesis followed
    by a closing parenthesis at some point.
'''
regex = r'.*\(.*\).*'
